// Library export — consumed by user-authored `hardened.config.ts` files.
export { defineConfig } from "@hardened/core"
export type {
  Rule,
  Match,
  Fix,
  ResolvedConfig,
  UserConfig,
  Severity,
  SeverityOrOff,
  Category,
} from "@hardened/core"
